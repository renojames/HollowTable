

<h2>Create a New Game</h2>

<p>NOTE: Please carefully review the list of existing games before creating a new one!</p>

<p style="font-weight: bold;">Enter the new game info:</p>

<form action="/games" method="post">

  <p>Game Title: <input type="text" name="name" placeholder="The full title of the game"></p>
  <p>Game Summary:</P>
  <textarea name="summary" placeholder="A short summary of the game's themes" rows="8" cols="40"></textarea>
  <p>Max Number of Players: <input type="number" name="max_players" placeholder="e.g. '6'"></p>
  <p>Play Time: <input type="text" name="play_time" placeholder="e.g. '30-90 minutes'"></p>
  <p>Year Published: <input type="number" name="year_published" placeholder="e.g. '2001'"></p>

  <h3>Publisher:</h3>
  <% Publisher.all.each do |publisher| %>
    <label><input type="radio" name="publisher_id" value="<%= publisher.id %>"> <%= publisher.name %> </label>
    <br>
  <% end %>
  <p>Create New Publisher: <input type="text" name="new_publisher"></p>
  <p>Note: If an existing publisher is selected, the new publisher will not be created.</p>

  <h3>Genre(s):</h3>
  <p>Create New Genre: <input type="text" name="new_genre"></p>
  <p>Select Existing Genres:</p>
  <% Genre.all.each do |genre| %>
    <label><input type="checkbox" name="genres[]" value="<%= genre.id %>" > <%= genre.name %></label>
    <br> 
  <% end %>

  <h3>Play Style(s):</h3>
  <p>Create New Play Style: <input type="text" name="new_type"></p>
  <p>Select Existing Play Styles:</p>
  <% Type.all.each do |type| %>
    <label><input type="checkbox" name="types[]" value="<%= type.id %>"> <%= type.name %> </label>
    <br>
  <% end %>

  <br>
  <input type="submit" value="Save New Game">

</form>
